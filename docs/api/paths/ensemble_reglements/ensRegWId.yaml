delete:
  summary: Supprssion d'un entête d'ensemble de règlement existant
  deprecated: false
  tags: ['Ensembles de Règlements']
  parameters:
    - name: id_er
      in: path
      description: identifiant de l'ensemble de règlements qu'on désire supprimer
      required: true
      example: 75
      schema:
        type: number
  responses:
    '200':
      description: 'Succès'
      content:
        application/json:
          schema: 
            $ref: '../../components/schemas.yaml#/api_response'
          example:
            success: true
    '500':
      description: 'Erreur Serveur'
      content:
        application/json:
          schema: 
            $ref: '../../components/schemas.yaml#/api_response'
          example:
            success: false
            message: 'Erreur interne serveur'